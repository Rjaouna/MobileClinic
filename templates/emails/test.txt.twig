TEST DES NOTIFICATIONS {{ brand_name|upper }}

Les notifications sont correctement configurées.
Ce message valide le serveur d’envoi, le modèle graphique, le logo et les coordonnées de la boutique.

{{ site_url }}
{{ setting.storeAddress }}
{{ setting.storePhone }}
{{ setting.storeOpeningHoursSummary }}
